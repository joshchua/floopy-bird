const mainMenuDiv = document.querySelector(".mainMenu");
const gameOverDiv = document.querySelector(".gameOver");
const hudDiv = document.querySelector(".score");

const startGameBtn = document.querySelector(".mainMenu .startGame");
const playAgainBtn = document.querySelector(".gameOver button");
const githubBtn = document.querySelector(".mainMenu .github");

const scoreDisplays = document.querySelectorAll(".score");

export {
  mainMenuDiv,
  gameOverDiv,
  hudDiv,
  startGameBtn,
  playAgainBtn,
  githubBtn,
  scoreDisplays
};
